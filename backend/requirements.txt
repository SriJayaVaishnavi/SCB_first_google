google-adk>=1.0.0
google-genai>=0.3.0
google-cloud-aiplatform>=1.60.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.6.0
python-dotenv>=1.0.0
# litellm: ADK's bridge to non-Gemini models — only needed for BEACON_MODE=groq (temp dev backend).
litellm>=1.50.0
# Pin websockets to ADK's range — litellm otherwise pulls 16.0, which google-adk rejects.
websockets>=15.0.1,<16.0.0
pytest>=8.0.0
