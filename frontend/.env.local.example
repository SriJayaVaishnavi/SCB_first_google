# Copy to .env.local. Where the FastAPI backend is reachable from the browser.
# Local dev: http://localhost:8000
# Cloud Shell Web Preview / Cloud Run: the deployed backend URL.
NEXT_PUBLIC_API_URL=http://localhost:8000
